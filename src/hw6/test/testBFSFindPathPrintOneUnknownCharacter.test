# Test BFS finding a path,
# it prints one "unknown character notFlower" line, if notFlower is not in graph.

# load graph
LoadGraph g flowers.tsv

# find a path with dest node no in the graph
FindPath g notFlower Aconitum